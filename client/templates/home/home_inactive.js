Template.homeInactive.helpers(	{    lastActiveStatusDescription: function() {      var lastItem = History.find({}, {          limit: 1,          sort: {              timeStamp: -1          }        }),        fetchedItem,        fetchedItemMoment;      if (!lastItem.count()) {        return {          description: 'ever'        };      }      fetchedItem = lastItem.fetch()[0];      fetchedItemMoment = new moment(fetchedItem.timeStamp);      return {        description: 'since ' + fetchedItemMoment.from(Date.now())      };    },		history: function() {			return History.find({},				{					sort: {						timeStamp : -1					},					limit: 5				}			);		}});